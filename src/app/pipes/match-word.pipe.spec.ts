import { MatchWordPipe } from './match-word.pipe';

describe('MatchWordPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchWordPipe();
    expect(pipe).toBeTruthy();
  });
});
